import { IPlaylist } from "../../models/playlist.model";

interface Props {
  playlist: IPlaylist;
}
export default function PlaylistIndexListPreview({playlist}: Props) {
  return <div>PlaylistIndexListPreview</div>;
}
